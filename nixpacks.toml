[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[phases.build]
cmds = ["cp -r frontend backend/frontend_dist"]

[start]
cmd = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
